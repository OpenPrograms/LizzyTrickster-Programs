{
    ["dnsd"] = {
      files = {
            ["master/dns-server.lua"] = "/bin"
      },
      name = "DNS Daemon",
      description = "Manages DNS style entries for UUID addresses",
      authors = "JoshTheEnder",
      note = "Incomplete and may contain bugs. Use at own risk",
    }
}
